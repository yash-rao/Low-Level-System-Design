## CREATIONAL DESIGN PATTERN : SINGLETON PATTERN 

The Singleton Pattern is a creational design pattern that ensures a class has only one instance and provides a global access point to that instance.
It is widely used to manage shared resources or centralized configurations.


We can make sure again if lazy initialization does not work we can use synchronized key word in method to make it thread safe.
